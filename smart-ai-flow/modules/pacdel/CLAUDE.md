# Módulo: PACDEL

> Este arquivo é o briefing que a IA recebe em toda análise/proposta deste
> módulo. Quanto mais preciso, menos alucinação. Trate como documento vivo:
> cada bug bem resolvido vira uma linha nova aqui.

## Visão geral

Módulo do SMART Desktop entregue na biblioteca `pacdel` (91 arquivos
versionados em `ws_objects/pacdel*/`).

<!-- preencher: escopo funcional, quem usa, telas principais. -->

## Onde o código mora

`ws_objects/<lib>/<lib>.pbl.src/<objeto>.sr?` — confirme o caminho exato pela
seção "Arquivos reais" do prompt antes de propor diff. Nunca deduza pasta a
partir do nome do módulo.

## Objetos-chave

<!-- preencher: os objetos que mais aparecem em chamados deste módulo.
     Use `GET /search?q=<termo>` no PB Insight pra descobrir o caminho real. -->

## Armadilhas conhecidas

- Vale tudo do briefing do sistema (`modules/smartdesktop/CLAUDE.md`): Oracle ×
  SQL Server, parâmetros INI, objetos compartilhados de `aplgen50`, e o padrão
  `avancar`/`i_bAvancar` do cadastro de paciente.

<!-- preencher conforme os chamados forem resolvidos. -->
