# Estes dois são bootstrap: o processo precisa deles pra sequer conseguir
# falar com o banco, então não tem como virar configuração-no-banco.
DATABASE_URL=postgresql://smart:smart@localhost:5432/smart_ai_flow
PORT=3333

# Tudo mais (chave Anthropic/OpenAI, provider ativo, Jira, URL do app_trace,
# URL do PB Insight) mora na tabela PlatformSettings agora — configure pela
# tela de Configurações da plataforma (não precisa reiniciar o backend depois).
